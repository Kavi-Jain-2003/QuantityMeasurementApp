spring:
  datasource:
    url: jdbc:mysql://localhost:3306/quantity_measurement_db
    username: root
    password: ckjain1166

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQLDialect
